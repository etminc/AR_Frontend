{
  "name": "etm-ar-collections-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/api.js && node --check src/format.js && node --check src/mock-data.js && node --test && vite build"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
