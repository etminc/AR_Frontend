{
  "name": "etm-ar-collections-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/api.js && node --check src/format.js && node --check src/mock-data.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
